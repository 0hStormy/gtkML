<img width="100%" src="assets/logo_dark.svg">
A GTK 4 runtime that uses an HTML-like XML language along with CSS for style and Python for logic.

### How to use

Currently you just move the `main.py` file into your app folder, create a file called `logic.py`, and a file called `ui.gtkm`. You can use the example files for help.